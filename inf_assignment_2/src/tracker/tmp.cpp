==========\n"; // aggiungo qui tutte le track std::vector<Tracklet> total_tracks; auto active = tracker.getTracks(); //attive total_tracks.insert(total_tracks.end(), active.begin(), active.end()); auto lost = tracker.getLostTracks(); //perse recentemente total_tracks.insert(total_tracks.end(), lost.begin(), lost.end()); auto archived = tracker.getArchivedTracks(); //perse definitivamente total_tracks.insert(total_tracks.end(), archived.begin(), archived.end()); // Per ogni track stampa la sua storia dei passi for (auto &t : total_tracks) { std::cout << "Track ID " << t.getId() << " (" << t.getHistory().size() << " points)"; const auto &history = t.getHistory(); for (int i = 0; i < history.size(); i++) { std::cout << " [" << i << "] X=" << history[i].x << ", Y=" << history[i].y << "\n"; } std::cout << "------------------------------------------\n"; } std::cout << "==========================================\n"; }